# This is a basic workflow that is manually triggered

name: Automate deploy

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'A form to automate deploy to Droplet of DigitalOcean'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Get access to cloud ☁️
      run: ssh root@164.92.100.17

    - name: Enter to folder 🔒
    - run: cd /var/www/nonna-commercial

    - name: Pull changes of new version 🧬
    - run: git pull

    - name: End process 🫡
    - run: exit
